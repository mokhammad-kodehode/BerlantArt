// Схема базы данных.
//
// Пока здесь только провайдер — модели Artwork и Image появятся на этапе 3.
//
// Строк подключения тут намеренно нет: в Prisma 7 свойства url и directUrl
// в схеме больше не поддерживаются, адреса задаются в prisma.config.ts.

datasource db {
  provider = "postgresql"
}
