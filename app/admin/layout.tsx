import type { ReactNode } from "react";

/**
 * Каркас админки. Нужен ради одного класса на обёртке: по нему
 * `app/globals.css` переводит заголовки и кнопки на текстовый шрифт
 * (блок «админка»).
 *
 * Публичные страницы — витрина, там заголовки набраны антиквой
 * Oranienbaum. Админка — рабочий инструмент: художница смотрит в неё
 * не любоваться, а вносить работы, и мелкий высококонтрастный шрифт
 * с тонкими засечками читается там хуже.
 *
 * Обёртка охватывает и страницу входа: она тоже часть админки.
 */
export default function AdminLayout({ children }: { children: ReactNode }) {
  return <div className="admin-ui">{children}</div>;
}
