# Эталон макета. Правится только в исходном инструменте дизайна, поэтому
# переформатировать его нельзя: сверять код с макетом станет труднее,
# а diff распухнет на тысячи строк.
design/

# Сгенерированный клиент Prisma. Prettier и так пропускает его по .gitignore,
# но пусть будет явно: файл переписывается каждой командой `prisma generate`.
lib/generated/
